import 'package:flutter/material.dart';

class Espacamento extends StatelessWidget {
  const Espacamento({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 70,
    );
  }
}